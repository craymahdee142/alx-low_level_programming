#include "main.h"

/**
 * factorial - function that returns a factorial of a given num
 *@n: number to return
 * Return: factorial
 */
int factorial(int n)
{
	int res = n;

	if (n < 0)
		return (-1);
	else if (n >= 0 && n <= 1)
		return (1);
}
