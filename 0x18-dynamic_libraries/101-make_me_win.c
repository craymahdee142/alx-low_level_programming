#include <stdlib.h>
/**
 *malloc - allocate memmroy size
 *@size: memory size
 *Return: NULL
 */
void *malloc(size_t size)
{
	(void)size;
	return (NULL);
}
/**
 *rand - picks random number
 *Return: int
 */
int rand(void)
{
	return (9);
}
